import type { NextPage } from "next";
import { Card, Layout } from "@components";

const Home: NextPage = () => {
  return (
    <Layout pageTitle="Exotic Cars">
      <Card />
    </Layout>
  );
};

export default Home;
